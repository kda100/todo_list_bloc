name: todolist
description: A Todo list with firebase backend

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  firebase_core: ^1.15.0
  cloud_firestore: ^3.1.13



  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  provider: ^6.0.2
  firebase_core: ^1.15.0
  cloud_firestore: ^3.1.13
  intl: ^0.17.0
  flutter_keyboard_visibility: ^5.1.1

flutter:

  uses-material-design: true
